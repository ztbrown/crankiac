# Database connection string - REQUIRED
# Format: postgresql://user@host:port/database
# Example for local development:
DATABASE_URL=postgresql://postgres@localhost:5432/crankiac

# Patreon session ID for API access (optional, needed for fetching episodes)
PATREON_SESSION_ID=your_session_id_here

# YouTube Data API key (optional, enables fetching video duration)
# Get one at: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your_api_key_here

# Ollama configuration (optional, enables smart-search via LLM query expansion)
# Default URL is http://localhost:11434 if not set
OLLAMA_URL=http://localhost:11434
# Default model is llama3.2 if not set
OLLAMA_MODEL=llama3.2

# Server configuration (optional)
HOST=127.0.0.1
PORT=5000
DEBUG=false
